import 'package:flutter/material.dart';

class ReusableTextWidgets extends StatelessWidget{
  final String title;
  const ReusableTextWidgets({super.key, required this.title});

  @override
  Widget build(BuildContext context) {
    return Text('');
  }
}